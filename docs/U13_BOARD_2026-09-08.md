# U13 manual combat board — 2026-09-08

## Current entry point

The default board now opens the Lord/Castle picker. See
[U13_LOADOUT_BOARD_2026-09-08.md](U13_LOADOUT_BOARD_2026-09-08.md) for the current
Deimos/Gremory, Castle action and exercise-opening flow. The original Gremory
fixture described below remains covered by the legacy board regression runner;
`--dense` retains the accepted performance fixture.

## Original scope

`Prototype/U13/U13Board.tscn` is the first manual U13 board. It reuses
UI2's HandView (including subject art, selection and inspection), Gremory
portrait catalog and upper/lower Domain1 artwork. A separate controller binds
these surfaces to U13 player views and submissions. No U12 controller, scene,
rule source, asset or project main scene is changed.

This is still the explicit Gremory basic combat profile, not a complete U13
match. Choose Pass, Siege or Ward; select individual physical cards; queue
Predator of Ruin in either lane; reserve exactly two selected cards for
Inevitable Ruin targeting either Castle. Ruin payments disappear from the
available hand so they cannot also pay for combat. Clear powers returns them.
Every candidate passes U13Match.preview_submission (and therefore U13Legality
for powers); commit passes submit and the authoritative hook driver.

The setup remains the tested opening combat fixture: Gremory mirrors, two
plain Integrity Castles, Wright guards, four cards each and the shared Gremory
Breach. This does not implement Keep/Bastion powers, Development, Hunt, normal
round draws, economy progression or victory. Restart restores the opening
hand. Do not call this a complete playable match or use it for tuning.

## Opponent

U13BoardSession contains a scoped random-legal exercise chooser for both
implemented Gremory powers. It never reads the human submission or hand. It
uses the opponent's player view to generate candidates, and match preview to
filter them. One legal power type is chosen uniformly, then a canonical legal
payload for that power uniformly; no power pass if at least one is legal.
Sorted physical card pairs exclude payment permutations. With no legal power
it passes powers. Keys include round, player and decision/power; purpose keys
are BOT_POWER_CHOICE and BOT_POWER_TARGET. Combat uses BOT_COMBAT_CHOICE over
legal single-card/pair Siege/Ward orders; no legal candidate means combat pass.
The complete power-plus-combat plan is validated, preserving shared payment
and cooldown ownership. There is no strategic doctrine in this profile.

This starts the addendum's Gremory declaration path. Shared multi-Lord
enumeration, generalized doctrine dispatch, batch runners and frequency
instrumentation remain open. The fixed-seed manual board produces no win-rate,
balance or field-density claims. It does not replace a future real doctrine.

## Playback and exit

Every Commit & resolve runs to Marching, then animates its recorded tape,
including rounds without a clash. All suits use the same interpolator. New
committed Marchers hold in their birth round; Gremory summons move immediately.
Stats/events retain the pre-Marching view during animation; the resolved view
appears when it finishes. Skip animation completes Aftermath once. Input is
locked during playback; Restart cancels it. Rendering never feeds fractional
visual positions back into the fixed-point simulation.

The user reported Butchers jumping in the prior smoke UI. Its exact cause is
not established. New regression checks explicitly cover no-contact Butcher
midpoint/end positions and birth hold, plus scene commit/playback routing.
Godot visual confirmation on this board is still required before calling the
reported symptom resolved.

The user confirmed Alt+F4 exits without the engine cleanup messages produced by
Ctrl+C. Normal window close or the board's Exit button is the supported exit;
no simulation change was made for those interruption-only messages.

## Launch

From the U13 project folder:

```bash
bash Scripts/Sim/run_u13_board.sh "$u13_godot"
```

This skips all tests and opens the board directly. F6 on U13Board.tscn also
works; F5 continues to use the existing project main scene.

## Verification

Static parsing and lint checks cover the new scripts. Bash syntax and wrapper
shims cover the 13-runner completion gate and failure handling. Launcher shims
check paths containing spaces and explicit scene arguments. These are not
Godot execution or rendered visual checks.

The new U13BoardTestRunner checks payment separation and preview purity,
keyed opponent legality/repeatability, actual three-round deterministic owner
execution, no-contact Butcher interpolation and birth hold, and actual scene
hand selection/reservation/commit/skip/next/restart behavior. The foundation
wrapper now includes this thirteenth suite and direct script preflights.

**Local Godot 4.7.2 runtime verification is pending: expected 13/13.** No
runtime or visual pass is claimed from the coding environment.


## Direct-launch repair after local 21cb55d report

The first local launch exposed absent `.godot/imported/*.ctex` files plus
reversed arguments in U13DomainRow.draw_texture_rect_region. The call now uses
Godot's documented texture, destination rectangle, source rectangle order.

U13BoardTextures decodes the source PNGs with Image.load and caches
ImageTextures, so running from a source checkout does not depend on an editor
import pass. This covers Domain1, BottomBanner, all subject faces/backs and the
Gremory portrait, not just the two assets named in the first error log. The
loader uses the existing UI2 path catalogs. U13BoardHand is a frozen copy of
UI2 HandView with only its class name and texture loading adapted, preserving
selection, fan layout, inspection and input behavior. The original UI2 sources
are unchanged. Future shared-component extraction should keep those behaviors.
This source-PNG route is for the development board; exported builds must either
include source PNG files or supply an imported-asset loading route.

The board runner checks source textures and cache reuse before the scene
checks. Static verification confirms the board's presentation dependencies no
longer preload PNG resources. Godot 4.7.2 runtime verification remains pending.

API references: [CanvasItem drawing](https://docs.godotengine.org/en/stable/classes/class_canvasitem.html#class-canvasitem-method-draw-texture-rect-region)
and [ImageTexture](https://docs.godotengine.org/en/stable/classes/class_imagetexture.html).

## Audited decision-flow reuse

The toolbar is replaced by extracted UI2 PhasePrompt and ActionZone presentation
in `U13PhasePrompt.gd` and `U13ActionZone.gd`. PhasePrompt retains its original
expanded/collapsed layout, DecisionPanel texture style, normalized header and
bottom-action slots, sibling overlay handling and View Board/Return behavior.
ActionZone retains its scroll/content hierarchy, action-button construction,
target widgets, status display and confirm/pass signal conventions. U12 rule
bindings, legality, forecast and phase-dispatch methods are not included.

U13Board supplies the commitment description and owner-preview result. Visible
Siege, Ward and Powers Only choices replace the action dropdown. Confirm sends
the selected combat order plus queued powers. Pass Round explicitly cancels
queued powers and skips combat; Powers Only + Confirm retains those powers.
Missing Siege targets have plain-language feedback and cannot disable Pass.
Opening/collapsing the decision does not rebuild the hand or clear payment.
The hand stays clickable outside the prompt. Playback hides the prompt and its
sibling buttons; Aftermath uses its existing bottom action slot for Next Round.

This implements the audit's first decision-flow step. The rest of the current
board remains the limited domain/lane presentation: full PlayerBoard/card,
drag-target, scoreboard and battlefield/theater adaptations are still pending.
No full UI migration or runtime/visual pass is claimed by this patch.

The existing board runner now exercises actual ActionZone button dispatch,
visible Pass, no-target feedback, View Board/Return with retained reservations,
Pass cancellation, playback hiding and modal Next Round. The wrapper remains
13 suites and adds direct preflights for the two extracted scripts. The source
texture checks include DecisionPanel. Static parsing and wrapper checks are
available in the coding environment; Godot 4.7.2 validation remains local.

## Full U12 board layout port (after b8259e4)

The compact test-board strips are replaced by the old UI2 board composition:

- 1920×1080 canvas, initially shown in a 1440×810 window.
- LordPanel score frames, TopBanner center strip and a Breach card across the top.
- Two stacked domains with 188×282 Lord cards, vertical Lord Guard slots,
  a reserved prompt gutter, five Castle card positions and Castle Guard rows.
  Enemy Castles sit above their Guards; the human row reverses that order.
- A 290-pixel right-hand Battlefield rail with the original artwork/crop,
  an Action window and vertical Lord/Castle tracks. Human Marchers travel up;
  enemy Marchers travel down. U13 playback still owns all recorded positions.
- The existing UI2 hand treatment along the bottom, with History behind a button.
- The original decision artwork, fixed at 400×530, with an internal scroll area,
  visible status, Confirm/Pass windows and View Board/Return behavior.

`U13PlayerBoard` ports the UI2 PlayerBoard layout methods and ordering. The
U13 adapter binds only the current owner's public view. `U13LayoutCard` reuses
UI2's hold-to-inspect component; repeated refreshes update its texture rather
than creating extra preview layers. Clicking the human Lord selects Ward/Lord;
clicking a live Castle selects Siege or Ward/Castle and preserves selected cards.

This restores the board layout, not missing U12 rules or its drag/drop controller.
The first Castle position represents the current plain U13 test Castle, explicitly
labelled TEST CASTLE. Other named Castle positions are dimmed and inactive.
Printed Keep powers do not execute. The header reports the published Neutral Tear
count, not a fabricated total Veil or victory state. Development, Hunt, draws,
full named Castle powers and victory remain outside this checkpoint.

Two reported launch problems are addressed:

1. First paint checks `has_meta` before reading the collapsed-panel metadata.
2. Source PNGs are decoded from bytes with `load_png_from_buffer`, avoiding
   `Image.load`'s source-checkout export warnings. When source bytes are absent,
   the helper can load an imported Texture2D instead. Export packaging is not
   verified by this source-checkout change.

### Verification for this port

GDScript grammar/format checks and Bash syntax/wrapper shim checks were run.
The shims test orchestration and error detection; they do not execute game rules.
The Board runner now checks the added artwork, five Castle positions, Guard
ordering, right rail, hand bounds, modal separation, repeated preview reuse,
and click-to-target selection, alongside the existing Pass/payment/playback tests.

Godot is unavailable in the editing environment. Engine compilation, rendered
layout and these new assertions remain pending local Godot 4.7.2 execution.
No new 13/13 result is claimed. The board launcher runs only the scene; the
foundation wrapper still runs 13 suites and now preflights all new UI adapters.

## Combat, then Lord powers (same submission window)

The board now presents two separate modal steps using the same artwork:

1. **Combat**: stage Siege/Ward and its cards, then choose Next — Lord Powers.
   Skip Combat still opens the powers step.
2. **Lord Powers**: choose powers using only cards not reserved for combat.
   Resolve Round submits the combined order. No Powers clears powers but keeps
   the staged combat. Back to Combat clears powers and restores combat selection.

Switching prompts calls no runtime hook. Both decisions remain editable before
one joint submission. Duplicate power clicks are no-ops; a queued power is labelled
and disabled. Current cooldown and ready round come from public owner clocks.

Ruin's UI explains its two-card cost and next-round firing. It offers damaged,
standing enemy Castles, excluding already-defunct targets that would show no
further effect. Aftermath explicitly reports the armed firing round. Its existing
rule remains: fire at ROUND_START_SCHEDULED next round, set Integrity to zero and
make the target defunct; this does not itself destroy the Castle or award a Tear.

The Board runner exercises no state advance between prompts, reserved combat
cards, separate Ruin payment, duplicate Predator clicks, Back/Skip controls,
cooldown countdown, and actual Ruin declaration through firing at the next round.
Grammar checks pass in the editing environment; Godot 4.7.2 execution is pending.

Movement follow-up audit: UI2's spread and center exclusion were presentation
properties. MarchingEngine resolved contact using lane distance, and the renderer
then steered the recorded opponents into visible contact. No movement changes are
included in this modal patch. Making lateral position authoritative needs an
explicit collision model rather than copying the UI's visual offsets into rules.


## Shared random-legal chooser and measured batches

After the accepted responsive-board performance pass, `U13BoardSession` delegates
opponent planning to `U13RandomLegal` with `U13GremoryCandidates`. Both sides in
headless batches use this same path. The new `U13_RANDOM_LEGAL_V1` policy sorts and
deduplicates complete legal payloads and keys choices to the actual match seed.
Individual bot decisions can differ from the old board-only policy; preview, costs,
cooldowns, submissions and firing legality remain authoritative in the match owner.
See [the batch scope and local acceptance commands](U13_RANDOM_BATCH_2026-09-08.md).
